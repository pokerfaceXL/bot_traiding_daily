# Outcome

`data_contract.py` implements the F003 contract as a standalone module: `filter_closed_candles` (closes F001's rozbieżność #5), `detect_gaps`, `split_warmup`, `build_manifest` (SHA-256 checksum + coverage%), `enforce_no_silent_gaps` (hard `DataGapError` unless explicitly opted in — no zero-fill), and `save_dataset`/`load_dataset` cache keyed by symbol/interval/UTC range with checksum verification on read. 11 tests in `tests/test_data_contract.py` pass against a hand-built fixture with a deliberate gap (`tests/fixtures/ohlcv_sample_gap.csv`), including a discriminating mutation test proving the closed-candle check isn't vacuous. `main.py`/`trader.py`/`configuration/` untouched.

**Scope gap, disclosed by the worker itself:** the worker's worktree branched from a stale base commit (Coordinator had not yet committed F001/F002 to git when the job was spawned — fixed as part of this merge), so it built `data_contract.py` as an independent module rather than wiring it into `backtest_apex.py`/`strategy.get_bybit_ohlcv` as the ticket scoped. That integration, plus instrument-basket selection and per-strategy warm-up sizing, remain open — see "Czego brakuje" in `spec/research/F003-data-contract.md`.
